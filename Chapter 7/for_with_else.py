name = 'Ravleen'
for i in name:
    if(i=='l'):
        break
    print(i)

else:
    print("Finished")

#The else part executes when the for loops works properly
#if it breaks it won't work, so use it likewise to check the completeness of for loop